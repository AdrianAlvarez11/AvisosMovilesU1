<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AvisosApp.Views.RegisterView"
             Title="" Background="#dad7cd">
    <VerticalStackLayout VerticalOptions="Center">
        <Label FontFamily="Verdana" FontAttributes="Bold" TextColor="Black" Text="Registrar" FontSize="24" HorizontalOptions="Center" Margin="0,0,0,50" />
        <Entry FontFamily="Verdana" Text="{Binding Maestro.Nombre}" Placeholder="Nombre" Background="#a3b18a"  Margin="10, 10, 10, 0"></Entry>
        <Entry FontFamily="Verdana" Text="{Binding Maestro.NumControl}" Placeholder="Numero de control" Background="#a3b18a"  Margin="10, 10, 10, 0"></Entry>
        <Entry FontFamily="Verdana" Text="{Binding Maestro.Contrasena}" Placeholder="Contraseña" Background="#a3b18a" Margin="10, 10, 10, 0"></Entry>
        <Entry FontFamily="Verdana" Text="{Binding Maestro.NombreGrupo}" Placeholder="Nombre del grupo" Background="#a3b18a" Margin="10, 10, 10, 0"></Entry>
        <Grid ColumnDefinitions="*, auto" Margin="10, 10, 10, 0" >
            <Label FontFamily="Verdana" TextColor="Black" Text="¿Ya estas registrado?" VerticalOptions="Center"></Label>
            <Button FontFamily="Verdana" Grid.Column="1" Text="Iniciar Sesión" TextColor="#344e41" Padding="0,0,5,0" Command="{Binding VistaLoginCommand}"
                    BackgroundColor="Transparent" VerticalOptions="Center"></Button>
        </Grid>
        <Label Margin="10, 10, 10, 0" FontFamily="Verdana" TextColor="Red" Text="{Binding Error}" IsVisible="{Binding HayError}"></Label>

        <Button Text="Registrar" Command="{Binding RegistrarMaestroCommand}" FontFamily="Verdana" Margin="10, 10, 10, 0"></Button>
    </VerticalStackLayout>
</ContentPage>
